---
title: "Test Quarto Document"
format: html
jupyter: python3
---

## Introduction

This is a test Quarto document to verify that our Neovim configuration works properly.

## Python Code

```{python}
import pandas as pd
import numpy as np

# Create some sample data
data = pd.DataFrame({
    'x': np.random.randn(100),
    'y': np.random.randn(100)
})

print(data.head())
```

## R Code

```{r}
# Create some sample data in R
x <- rnorm(100)
y <- rnorm(100)

# Create a simple plot
plot(x, y, main="Random Data", xlab="X values", ylab="Y values")

# Summary statistics
summary(data.frame(x, y))
```

## Mixed Analysis

You can also mix languages and reference variables across chunks when using the same kernel.